[pytest]
markers =
    unit: pruebas unitarias de un solo módulo
    integration: pruebas que cruzan límites de módulos
    contract: pruebas de contratos públicos (OpenAPI, schemas)
    e2e: pruebas end-to-end de flujo completo
    eval: pruebas de evaluación de calidad (judge, RAG)
