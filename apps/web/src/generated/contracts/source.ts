/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Los cinco namespaces iniciales (§2.3). Un dominio fuera de esta lista se rechaza.
 */
export type Domain = "vehiculos" | "ayuntamiento_empresas" | "registro_civil" | "salud" | "ganaderia";
export type InstitutionId = string;
/**
 * Marca el contenido sintético de demo frente al institucional autorizado.
 */
export type IsSynthetic = boolean;
export type License = string;
export type OriginUrl = string | null;
/**
 * Responsable interno de la fuente.
 */
export type Owner = string;
export type Publisher = string;
export type SourceId = string;
/**
 * Estado de una fuente documental. Solo `ACTIVE` alimenta el retrieval.
 */
export type SourceStatus = "draft" | "active" | "superseded" | "expired";
export type Title = string;
export type ValidFrom = string;
export type ValidTo = string | null;
export type VerifiedAt = string | null;

/**
 * Fuente institucional registrada (§5.3).
 *
 * Los metadatos obligatorios son los que permiten auditar una respuesta: sin
 * institución, responsable, licencia y vigencia, una fuente no puede activarse
 * (`DIE-F2-022`).
 */
export interface Source {
  domain: Domain;
  institution_id: InstitutionId;
  is_synthetic?: IsSynthetic;
  license: License;
  origin_url?: OriginUrl;
  owner: Owner;
  publisher: Publisher;
  source_id: SourceId;
  status?: SourceStatus;
  title: Title;
  validity: ValidityWindow;
  verified_at?: VerifiedAt;
}
/**
 * Ventana de vigencia de una fuente o una regla.
 *
 * `valid_to` nulo significa vigencia abierta. El retriever filtra por esta
 * ventana antes de entregar texto a un agente (`DIE-F1-022`).
 */
export interface ValidityWindow {
  valid_from: ValidFrom;
  valid_to?: ValidTo;
}
